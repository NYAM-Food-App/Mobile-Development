<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NYAM" parent="Theme.Material3.DayNight">
        <!-- Customize your dark theme here. -->
         <item name="colorPrimary">@color/main_color</item>
<!--        <item name="actionBarStyle">@style/MyActionBarNight</item>-->
<!--        <item name="android:windowActionBarOverlay">true</item>-->
<!--        <item name="windowActionBarOverlay">true</item>-->
        <item name="colorPrimaryContainer">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="buttonStyle">@style/ButtonColor</item>
        <item name="android:buttonStyle">@style/ButtonColor</item>
    </style>

    <style name="ButtonColor" parent="@style/Widget.AppCompat.Button">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="MyActionBarNight" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/transparent_black</item>
    </style>
</resources>